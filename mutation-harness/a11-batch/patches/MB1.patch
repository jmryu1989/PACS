diff --git a/worklist-v0/hpacs-lite/viewer-volume-mip.js b/worklist-v0/hpacs-lite/viewer-volume-mip.js
index ad0252acdf310346dcbbfec84c7e2eed0cc28d83..6dacacfec8a2df4140290583872304d1097c198d 100644
--- a/worklist-v0/hpacs-lite/viewer-volume-mip.js
+++ b/worklist-v0/hpacs-lite/viewer-volume-mip.js
@@ -227,7 +227,7 @@
     // A turn about a fixed viewUp and focal point keeps the previous slab planes (viewer-volume-orientation.js:1-3), so the whole-volume
     // slab is re-derived from this frame's camera; the VOI Slab planes are then written again in source LPS, never from the camera.
     run.view.setSlabThickness(op.thickness/2);
-    writeVoi(op,request.original?null:request.voiSlab??null,run.mapper);
+    writeVoi(op,request.original?null:(request.voiSlab?{...request.voiSlab,normal:camera.index?[...camera.viewPlaneNormal]:request.voiSlab.normal}:null),run.mapper);
   }
   function verifyBatchFrame(op,run,camera,request){
     batchCheck(op,run);binding(op);
