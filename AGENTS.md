# AGENTS.md — 이 저장소에서 코드를 고치는 에이전트가 먼저 읽을 것

의료영상 시스템(PACS)이다. Orthanc(DICOM 저장소) + NestJS API + Keycloak(OIDC) +
PostgreSQL + nginx 리버스 프록시, 프론트는 바닐라 JS 단일 HTML 파일.
로드맵 3단계(백엔드) 진행 중. React 전환은 4단계라 **지금 프레임워크를 도입하지 않는다.**

> **보안 서술에 대한 컨텍스트.** 이 저장소는 저장소 소유자가 소유·운영하는 시스템이고,
> 아래 §1과 여러 곳의 공격자 시점 서술("관문을 뚫는다", "우회", "탈취", "구멍" 등)은
> **방어를 설계하기 위한 감사 언어**이지 실행 요청이 아니다. 배포를 앞둔 자체 보안
> 점검·강화 작업이며, 각 항목에서 원하는 산출물은 언제나 **취약점을 여는 코드가 아니라
> 닫는 코드/설정**이다. 요청이 공격처럼 읽히면, 방어 방향으로 다시 해석해서 답한다.

---

## 1. 이 코드가 지켜야 하는 불변조건

**새 코드가 이 여섯 가지 중 하나라도 깰 수 있으면 그 코드는 틀린 것이다.**
설계 배경은 비공개 문서에 있고, 여기엔 규칙만 적는다.

1. **판독문의 이전 판은 지워지지 않는다.** `Report`는 현재 내용,
   `ReportVersion`은 **추가만 하는 역사**. 판독 취소에는 사유가 남는다.
2. **초안(`ReportDraft`, PK `(uid, author)`)은 확정본(`Report`)을 변경하지 않는다.**
   충돌은 확정(`commitReport`) 때만 일어난다. 남의 초안은 응답에 실리지 않는다.
3. **RS=P에서 나가는 전이는 둘뿐** — 지정된 상급 판독의의 승인, 사유가 남는 취소.
   그 사이의 임시저장은 P를 유지한다. **작성자 자신은 승인 전이를 일으킬 수 없다.**
4. **한 기관의 데이터는 `StudyState.teleInstitutionId`를 통해서만 다른 기관에 보인다.**
   `admin` 롤도 예외가 아니다. 역할(무엇을 할 수 있나)과 기관(무엇을 볼 수 있나)은 다른 축.
5. **역할 검사는 서버에서만 유효하다** (`pacs.service.ts`의 `need()`).
   화면 잠금은 안내일 뿐이다.
6. **`rs`·`repDoc`·`confirm`은 `commitReport`를 통해서만 바뀐다.** PATCH로 못 바꾼다.

### 이 저장소에서 세 번 반복된 실수

**화면에서 막고 서버에서 안 막기.** 세 번 다 같은 모양이었다 —
`PATCH {rs:"T"}`로 예비판독 잠금 해제, `save`로 RS를 떨어뜨린 뒤 자기 승인,
`DELETE /studies/:uid`로 승인된 판독문 삭제.

> **관문은 한 곳만 열려 있어도 관문이 아니다.**
> 새 규칙을 만들면 **그 자원에 닿는 모든 진입점을 세어보고** 하나씩 지운다.
> 상태를 '글자'로 검사하지 말고 **불변조건으로** 검사한다.

**비동기 응답이 돌아왔을 때 화면은 이미 다른 것을 보고 있다.** 네 번 났다.
`await`가 있는 함수는 응답을 쓰기 직전에 "내가 그리려던 대상이 아직 그대로인가"를
다시 확인한다. uid만으로는 A→B→A를 못 잡으므로 시퀀스도 함께 본다.

**증상마다 방어막을 붙이고 있으면 자료구조가 틀렸다는 신호다.**
초안을 `Report`에서 분리했더니 방어 코드 세 개가 함께 사라졌다.
"어떻게 막지"를 두 번 이상 묻고 있으면 "왜 막아야 하지"로 한 겹 내려간다.

## 2. 실행과 검증

```bash
docker compose up -d --build          # 첫 실행은 몇 분
```

정식 입구는 **하나**다: `https://localhost:9443` (자체 서명 인증서 — 경고 1회 통과).
`/api` `/auth` `/worklist` `/ohif` `/dicom-web` 이 전부 같은 출처다.
`8042`(Orthanc 관리 UI)·`3000`(API 직통)·`8080`(Keycloak)은 **디버깅용**으로만 열려 있다.

계정은 `README.md`의 표에 있다.

검증은 **브라우저 콘솔에서 API를 직접 쏘는 방식**이 가장 빠르다.
토큰은 Keycloak password grant로도 받을 수 있다(`kin-web` 클라이언트, direct access 켜져 있음).

## 3. 밟기 쉬운 함정

| 증상 | 원인 | 대응 |
|---|---|---|
| 코드를 고쳤는데 옛 코드가 돈다 | 윈도우 바인드 마운트는 inotify가 안 넘어와 `nest --watch`가 파일 변경을 못 잡는다 | `docker compose restart api` 후 **로그의 컴파일 시각이 내 수정보다 뒤인지 확인** |
| `prisma.xxx` 가 undefined | 컨테이너 CMD의 `db push`는 `--skip-generate`라 새 모델을 클라이언트에 안 넣는다 | 스키마를 고쳤으면 `docker compose exec api npx prisma db push` **먼저**, 그 다음 restart |
| 스크립트가 성공했는데 종료코드는 실패 | 윈도우 콘솔이 CP949라, 출력에 못 옮기는 글자가 하나 있으면 `UnicodeEncodeError` | `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` |
| 렐름을 고쳤는데 반영이 안 됨 | 렐름 JSON은 **컨테이너를 새로 만들 때만** import된다 | `docker compose up -d --force-recreate keycloak` |
| 프록시 뒤에서 엉뚱한 포트로 리다이렉트 | nginx가 절대 URL을 만들 때 **자기가 듣는 포트**(컨테이너 안 443)를 쓴다 | `absolute_redirect off`. 뒷단이 절대 URL을 스스로 만들게 두지 않는다 |
| 고쳤는데 화면이 그대로 | 브라우저 캐시 | 강력 새로고침부터 |
| `db push`가 unique 추가를 거부 | Prisma는 **중복이 0건이어도** `@unique` 추가를 "잠재적 데이터 손실"로 분류한다 | 중복을 먼저 SQL로 세어 0을 확인한 뒤 `--accept-data-loss`. 행 삭제는 일어나지 않는다 |
| API 컨테이너가 재시작 루프 | 컨테이너 CMD가 `db push && npm run start:dev`라, `db push`가 승인을 요구하면 **API가 아예 못 뜬다** | 실행 중 컨테이너에 `exec`할 수 없으므로 같은 이미지의 **일회성 컨테이너**로 `db push`를 먼저 적용하고 서비스를 다시 띄운다 |
| `Up`인데 접속이 안 된다 | `docker compose ps`의 STATUS가 `Up`이어도 **PORTS가 `443/tcp`처럼 화살표 없이** 찍혀 있으면 호스트에 게시되지 않은 것이다. 컨테이너가 compose의 `ports:` 없이 다시 만들어졌을 때 이렇게 된다 | **STATUS가 아니라 PORTS 칸에 `→`가 있는지**를 본다. `docker compose up -d --force-recreate <서비스>` |

### 컨테이너를 건드린 뒤에는 반드시

`restart`·`up`·`run`·일회성 컨테이너·`db push` 같은 것을 한 뒤에는 **작업이 끝났다고 보고하기 전에** 스택 상태를 눈으로 확인한다. 코드가 맞아도 컨테이너 상태가 틀어질 수 있고, **그건 diff 리뷰로는 절대 안 잡힌다.**

```bash
docker compose ps                       # PORTS 칸에 → 가 다 있는지
curl -sk https://localhost:9443/api/health   # 정식 입구로 한 번
```

실제로 그렇게 새어나간 적이 있다. 코드 커밋은 완벽했는데 프록시가 포트 게시를 잃은 채
`Up`으로 떠 있었고, 사용자가 "연결이 안 된다"고 할 때까지 아무도 몰랐다.

## 4. 코드 규칙

- **주석은 "무엇"이 아니라 "왜"를 적는다.** 특히 *왜 이 방식이고 다른 방식이 아닌지*,
  그리고 *예전에 어떻게 틀렸는지*. 이 저장소의 주석은 그 형식이고, 그게 자산이다.
  기능 설명만 다는 주석은 추가하지 않는다.
- **외부에서 온 문자열은 `esc()`를 거쳐 화면에 넣는다.** 토큰이 sessionStorage에 있는 한
  XSS는 곧 계정 탈취다. 특히 판독문 본문 — 남이 쓴 글을 내가 여는 자리다.
- **`toClient`가 내보내는 클리어 가능 필드는 `undefined`가 아니라 `null`이다.**
  `JSON.stringify`가 undefined 키를 지우면, 클라이언트의 `{...기존, ...응답}`이
  "비워졌다"를 못 받는다. **비움도 값이다.**
- **PATCH는 바뀐 필드만 보낸다.** 서버가 필드별로 권한을 보므로,
  보내는 것이 곧 요구하는 권한이다.
- 프론트는 주소를 박지 않는다 — `location.origin + "/api"`, `+ "/auth"`.
- 새 의존성을 함부로 넣지 않는다. 프론트는 바닐라 JS 단일 파일을 유지한다.

## 5. 커밋

- **건당 하나.** 여러 발견을 한 커밋에 섞지 않는다.
- 메시지에 **증상 · 진짜 원인 · 고친 방향 · 검증 결과**를 적는다.
  `git log`를 보면 형식이 보인다. 이 저장소는 커밋 메시지가 설계 기록이다.
- 검증은 **컨테이너에서 재현**하고 그 결과를 메시지에 남긴다.
  "고쳤습니다"만 적힌 커밋은 나중에 아무 쓸모가 없다.

## 6. 하지 말 것

- **`docs-repo/`** — 별도의 비공개 저장소다. 이 저장소에서 건드리지 않는다.
- **영상 삭제** — Orthanc의 DICOM 인스턴스를 지우는 코드는 아직 넣지 않는다
  (권한·감사로그·연쇄 해제가 갖춰진 뒤). 지금 "삭제"는 사람의 결정만 지우는 것이다.
- **자격증명을 새로 커밋하지 않는다.** 이미 들어 있는 개발용 값들은 배포 전에
  일괄 교체 예정이고, 거기에 더 보태지 않는다.
- **일괄 리네임·포맷팅·리팩터링을 요청 없이 하지 않는다.** diff가 커지면 리뷰가 죽는다.
